exports.crearUsuario = async (event) => {
    let retorno = {
        statusCode: 200, body: { message: "", error: "" }
    }

    retorno = {
        ...retorno,
        body: JSON.stringify(retorno.body)
    }

    return retorno
}

exports.actualizarUsuario = async (event) => {
    let retorno = {
        statusCode: 200, body: { message: "", error: "" }
    }

    retorno = {
        ...retorno,
        body: JSON.stringify(retorno.body)
    }

    return retorno
}

exports.obtenerUsuariosDisponibles = async (event) => {
    let retorno = {
        statusCode: 200, body: { message: "", error: "" }
    }

    retorno = {
        ...retorno,
        body: JSON.stringify(retorno.body)
    }

    return retorno
}

exports.compararInversiones = async (event) => {
    let retorno = {
        statusCode: 200, body: { message: "", error: "" }
    }

    retorno = {
        ...retorno,
        body: JSON.stringify(retorno.body)
    }

    return retorno
}